apiVersion: v1
kind: Service
metadata:
    name: harinagexamplevotingappbretfisher
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: harinagexamplevotingappbretfisher